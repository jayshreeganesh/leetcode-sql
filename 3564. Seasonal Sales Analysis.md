# Seasonal Sales Analysis - Solutions

To solve the "Seasonal Sales Analysis" problem, we need to:
1.  **Map** `sale_date` to a season (Winter, Spring, Summer, Fall).
2.  **Aggregate** the total quantity and revenue for each category within each season.
3.  **Rank** the categories within each season based on `total_quantity` (descending), `total_revenue` (descending), and `category` (lexicographical ascending).
4.  **Filter** for the top-ranked category in each season.

---

## MySQL 8.0

```sql
WITH SeasonalSales AS (
    SELECT 
        CASE 
            WHEN MONTH(sale_date) IN (12, 1, 2) THEN 'Winter'
            WHEN MONTH(sale_date) IN (3, 4, 5) THEN 'Spring'
            WHEN MONTH(sale_date) IN (6, 7, 8) THEN 'Summer'
            ELSE 'Fall'
        END AS season,
        p.category,
        SUM(s.quantity) AS total_quantity,
        SUM(s.quantity * s.price) AS total_revenue
    FROM sales s
    JOIN products p ON s.product_id = p.product_id
    GROUP BY season, p.category
),
RankedSales AS (
    SELECT *,
           ROW_NUMBER() OVER(PARTITION BY season ORDER BY total_quantity DESC, total_revenue DESC, category ASC) as rn
    FROM SeasonalSales
)
SELECT season, category, total_quantity, total_revenue
FROM RankedSales
WHERE rn = 1
ORDER BY season;

```

---

## MS SQL Server 2019

```sql
WITH SeasonalSales AS (
    SELECT 
        CASE 
            WHEN MONTH(sale_date) IN (12, 1, 2) THEN 'Winter'
            WHEN MONTH(sale_date) IN (3, 4, 5) THEN 'Spring'
            WHEN MONTH(sale_date) IN (6, 7, 8) THEN 'Summer'
            ELSE 'Fall'
        END AS season,
        p.category,
        SUM(s.quantity) AS total_quantity,
        SUM(s.quantity * s.price) AS total_revenue
    FROM sales s
    JOIN products p ON s.product_id = p.product_id
    GROUP BY 
        CASE 
            WHEN MONTH(sale_date) IN (12, 1, 2) THEN 'Winter'
            WHEN MONTH(sale_date) IN (3, 4, 5) THEN 'Spring'
            WHEN MONTH(sale_date) IN (6, 7, 8) THEN 'Summer'
            ELSE 'Fall'
        END, 
        p.category
),
RankedSales AS (
    SELECT *,
           ROW_NUMBER() OVER(PARTITION BY season ORDER BY total_quantity DESC, total_revenue DESC, category ASC) as rn
    FROM SeasonalSales
)
SELECT season, category, total_quantity, total_revenue
FROM RankedSales
WHERE rn = 1
ORDER BY season;

```

---

## PostgreSQL 16

```sql
WITH SeasonalSales AS (
    SELECT 
        CASE 
            WHEN EXTRACT(MONTH FROM sale_date) IN (12, 1, 2) THEN 'Winter'
            WHEN EXTRACT(MONTH FROM sale_date) IN (3, 4, 5) THEN 'Spring'
            WHEN EXTRACT(MONTH FROM sale_date) IN (6, 7, 8) THEN 'Summer'
            ELSE 'Fall'
        END AS season,
        p.category,
        SUM(s.quantity) AS total_quantity,
        SUM(s.quantity * s.price) AS total_revenue
    FROM sales s
    JOIN products p ON s.product_id = p.product_id
    GROUP BY season, p.category
),
RankedSales AS (
    SELECT *,
           ROW_NUMBER() OVER(PARTITION BY season ORDER BY total_quantity DESC, total_revenue DESC, category ASC) as rn
    FROM SeasonalSales
)
SELECT season, category, total_quantity, total_revenue
FROM RankedSales
WHERE rn = 1
ORDER BY season;

```

---

## Oracle SQL 11.2

```sql
WITH SeasonalSales AS (
    SELECT 
        CASE 
            WHEN EXTRACT(MONTH FROM sale_date) IN (12, 1, 2) THEN 'Winter'
            WHEN EXTRACT(MONTH FROM sale_date) IN (3, 4, 5) THEN 'Spring'
            WHEN EXTRACT(MONTH FROM sale_date) IN (6, 7, 8) THEN 'Summer'
            ELSE 'Fall'
        END AS season,
        p.category,
        SUM(s.quantity) AS total_quantity,
        SUM(s.quantity * s.price) AS total_revenue
    FROM sales s
    JOIN products p ON s.product_id = p.product_id
    GROUP BY 
        CASE 
            WHEN EXTRACT(MONTH FROM sale_date) IN (12, 1, 2) THEN 'Winter'
            WHEN EXTRACT(MONTH FROM sale_date) IN (3, 4, 5) THEN 'Spring'
            WHEN EXTRACT(MONTH FROM sale_date) IN (6, 7, 8) THEN 'Summer'
            ELSE 'Fall'
        END, 
        p.category
),
RankedSales AS (
    SELECT season, category, total_quantity, total_revenue,
           ROW_NUMBER() OVER(PARTITION BY season ORDER BY total_quantity DESC, total_revenue DESC, category ASC) as rn
    FROM SeasonalSales
)
SELECT season, category, total_quantity, total_revenue
FROM RankedSales
WHERE rn = 1
ORDER BY season;

```

---

## Pandas (Python 3.10)

```python
import pandas as pd

def seasonal_sales_analysis(sales: pd.DataFrame, products: pd.DataFrame) -> pd.DataFrame:
    # Merge datasets
    df = sales.merge(products, on='product_id')
    
    # Calculate revenue
    df['revenue'] = df['quantity'] * df['price']
    
    # Map months to seasons
    month_to_season = {
        12: 'Winter', 1: 'Winter', 2: 'Winter',
        3: 'Spring', 4: 'Spring', 5: 'Spring',
        6: 'Summer', 7: 'Summer', 8: 'Summer',
        9: 'Fall', 10: 'Fall', 11: 'Fall'
    }
    df['season'] = pd.to_datetime(df['sale_date']).dt.month.map(month_to_season)
    
    # Aggregate
    grouped = df.groupby(['season', 'category']).agg(
        total_quantity=('quantity', 'sum'),
        total_revenue=('revenue', 'sum')
    ).reset_index()
    
    # Sort and pick top 1
    # For lexicographical smaller category in tie, we sort category ascending
    grouped = grouped.sort_values(
        by=['season', 'total_quantity', 'total_revenue', 'category'],
        ascending=[True, False, False, True]
    )
    
    result = grouped.groupby('season').head(1)
    
    return result[['season', 'category', 'total_quantity', 'total_revenue']]

```

