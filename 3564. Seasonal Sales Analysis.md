Here is the consolidated collection of all solutions we have discussed for the **Seasonal Sales Analysis** challenge.

---

# Seasonal Sales Analysis - Solutions

This document contains the verified solutions across the platforms requested, including SQL dialects and Python (Pandas).

## 1. MS SQL Server (T-SQL)

```sql
WITH SeasonalData AS (
    SELECT 
        CASE 
            WHEN MONTH(s.sale_date) IN (12, 1, 2) THEN 'Winter'
            WHEN MONTH(s.sale_date) IN (3, 4, 5) THEN 'Spring'
            WHEN MONTH(s.sale_date) IN (6, 7, 8) THEN 'Summer'
            ELSE 'Fall'
        END AS season,
        p.category,
        SUM(CAST(s.quantity AS BIGINT)) AS total_quantity,
        SUM(CAST(s.quantity AS BIGINT) * s.price) AS total_revenue
    FROM sales s
    JOIN products p ON s.product_id = p.product_id
    GROUP BY 
        CASE 
            WHEN MONTH(s.sale_date) IN (12, 1, 2) THEN 'Winter'
            WHEN MONTH(s.sale_date) IN (3, 4, 5) THEN 'Spring'
            WHEN MONTH(s.sale_date) IN (6, 7, 8) THEN 'Summer'
            ELSE 'Fall'
        END, 
        p.category
),
RankedCategories AS (
    SELECT 
        season, 
        category, 
        total_quantity, 
        total_revenue,
        ROW_NUMBER() OVER (
            PARTITION BY season 
            ORDER BY total_quantity DESC, total_revenue DESC, category ASC
        ) as rn
    FROM SeasonalData
)
SELECT season, category, total_quantity, total_revenue
FROM RankedCategories
WHERE rn = 1
ORDER BY season;

```

## 2. PostgreSQL

```sql
WITH SeasonalData AS (
    SELECT 
        CASE 
            WHEN EXTRACT(MONTH FROM sale_date) IN (12, 1, 2) THEN 'Winter'
            WHEN EXTRACT(MONTH FROM sale_date) IN (3, 4, 5) THEN 'Spring'
            WHEN EXTRACT(MONTH FROM sale_date) IN (6, 7, 8) THEN 'Summer'
            ELSE 'Fall'
        END AS season,
        p.category,
        SUM(s.quantity) AS total_quantity,
        SUM(s.quantity * s.price) AS total_revenue
    FROM sales s
    JOIN products p ON s.product_id = p.product_id
    GROUP BY season, p.category
),
RankedCategories AS (
    SELECT 
        season, 
        category, 
        total_quantity, 
        total_revenue,
        ROW_NUMBER() OVER (
            PARTITION BY season 
            ORDER BY total_quantity DESC, total_revenue DESC, category ASC
        ) as rn
    FROM SeasonalData
)
SELECT season, category, total_quantity, total_revenue
FROM RankedCategories
WHERE rn = 1
ORDER BY season;

```

## 3. Oracle PL/SQL

```sql
WITH SeasonalData AS (
    SELECT 
        CASE 
            WHEN EXTRACT(MONTH FROM sale_date) IN (12, 1, 2) THEN 'Winter'
            WHEN EXTRACT(MONTH FROM sale_date) IN (3, 4, 5) THEN 'Spring'
            WHEN EXTRACT(MONTH FROM sale_date) IN (6, 7, 8) THEN 'Summer'
            ELSE 'Fall'
        END AS season,
        p.category,
        SUM(s.quantity) AS total_quantity,
        SUM(s.quantity * s.price) AS total_revenue
    FROM sales s
    JOIN products p ON s.product_id = p.product_id
    GROUP BY 
        CASE 
            WHEN EXTRACT(MONTH FROM sale_date) IN (12, 1, 2) THEN 'Winter'
            WHEN EXTRACT(MONTH FROM sale_date) IN (3, 4, 5) THEN 'Spring'
            WHEN EXTRACT(MONTH FROM sale_date) IN (6, 7, 8) THEN 'Summer'
            ELSE 'Fall'
        END, 
        p.category
),
RankedCategories AS (
    SELECT 
        season, 
        category, 
        total_quantity, 
        total_revenue,
        ROW_NUMBER() OVER (
            PARTITION BY season 
            ORDER BY total_quantity DESC, total_revenue DESC, category ASC
        ) as rn
    FROM SeasonalData
)
SELECT season, category, total_quantity, total_revenue
FROM RankedCategories
WHERE rn = 1
ORDER BY season;

```

## 4. MySQL

```sql
WITH SeasonalData AS (
    SELECT 
        CASE 
            WHEN MONTH(s.sale_date) IN (12, 1, 2) THEN 'Winter'
            WHEN MONTH(s.sale_date) IN (3, 4, 5) THEN 'Spring'
            WHEN MONTH(s.sale_date) IN (6, 7, 8) THEN 'Summer'
            ELSE 'Fall'
        END AS season,
        p.category,
        SUM(s.quantity) AS total_quantity,
        SUM(s.quantity * s.price) AS total_revenue
    FROM sales s
    JOIN products p ON s.product_id = p.product_id
    GROUP BY season, p.category
),
RankedCategories AS (
    SELECT 
        season, 
        category, 
        total_quantity, 
        total_revenue,
        ROW_NUMBER() OVER (
            PARTITION BY season 
            ORDER BY total_quantity DESC, total_revenue DESC, category ASC
        ) as rn
    FROM SeasonalData
)
SELECT season, category, total_quantity, total_revenue
FROM RankedCategories
WHERE rn = 1
ORDER BY season;

```

## 5. Python (Pandas)

```python
import pandas as pd

def seasonal_sales_analysis(products: pd.DataFrame, sales: pd.DataFrame) -> pd.DataFrame:
    df = sales.merge(products, on='product_id')
    df['sale_date'] = pd.to_datetime(df['sale_date'])
    
    seasons = {1: 'Winter', 2: 'Winter', 12: 'Winter',
               3: 'Spring', 4: 'Spring', 5: 'Spring',
               6: 'Summer', 7: 'Summer', 8: 'Summer',
               9: 'Fall', 10: 'Fall', 11: 'Fall'}
               
    df['season'] = df['sale_date'].dt.month.map(seasons)
    df['revenue'] = df['quantity'] * df['price']
    
    grouped = df.groupby(['season', 'category']).agg(
        total_quantity=('quantity', 'sum'),
        total_revenue=('revenue', 'sum')
    ).reset_index()
    
    result = grouped.sort_values(
        by=['season', 'total_quantity', 'total_revenue', 'category'],
        ascending=[True, False, False, True]
    ).groupby('season').head(1)
    
    return result[['season', 'category', 'total_quantity', 'total_revenue']]

```
